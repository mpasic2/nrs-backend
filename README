Za pokretanje php fajlova potrebno je imati instaliran wamp server.

Sadržaj cijelog repozitorija (svi fajlovi i folderi) se trebaju ubaciti u C:\wamp64\www folder, a zatim pokrenuti wamp.
Kada je pokrenut wamp, potrebno je prvo učitati primjerak baze korisnici.sql koji je ubačen u projekat.
Učitavanje baze se radi na sljedeći način:
1. otvorimo browser 
2. upisujemo u adresu localhost/phpmyadmin
3. import

Kada smo učitali bazu, potrebno je da upišemo localhost u browser, te kad prepoznamo folder u kojem se 
nalazi naša web aplikacija upisujemo localhost/ime_našeg_foldera.

Sada možemo koristiti stranicu.